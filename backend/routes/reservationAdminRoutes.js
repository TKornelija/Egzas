// import express from "express";
// import { requireAuth, requireAdmin } from "../middleware/auth.js";
// import { listReservations, summaryReservations } from "../controllers/reservationController.js";

// const router = express.Router();
// router.get("/", requireAuth, requireAdmin, listReservations);
// router.get("/summary", requireAuth, requireAdmin, summaryReservations);

// export default router;
